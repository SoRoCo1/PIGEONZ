rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes (no Firebase Auth used — app handles auth via password)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
